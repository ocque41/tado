/*
 * Minimal C source so SwiftPM treats CTadoCore as a compilable target.
 * The real implementation is in the Rust static library linked in by the
 * Tado executable target.
 */
#include "tado_core.h"
