{
  "name": "tado-mcp",
  "version": "0.1.0",
  "description": "Legacy Node MCP reference; the public release uses the Rust tado-mcp binary.",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "tado-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
